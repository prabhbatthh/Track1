{
  "name": "may2026-team-041",
  "private": true,
  "scripts": {
    "backend": "git config core.hooksPath .githooks && python start_backend.py",
    "frontend": "git config core.hooksPath .githooks && npm --prefix frontend run dev",
    "test:e2e": "playwright test",
    "test:e2e:install": "playwright install --with-deps"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.45.0"
  }
}
